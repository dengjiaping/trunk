<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chat_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <include layout="@layout/thetopview"></include>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bg_color"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/itemHight"
            android:layout_marginBottom="@dimen/dp30"
            android:layout_marginTop="@dimen/dp30"
            android:background="@color/white"
            android:orientation="horizontal"
            android:paddingLeft="@dimen/itemMargin"
            android:paddingRight="@dimen/itemMargin">

            <TextView
                style="@style/textInMyFragmentLeft"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:text="班主任手机号" />

            <EditText
                android:id="@+id/our_addinclass_activity_edit"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_marginLeft="@dimen/itemMargin"
                android:background="@null"
                android:hint="请输入班主任的手机号"
                android:inputType="number"
                android:textColorHint="@color/text_gray_h2"
                android:textSize="@dimen/textSize" />

        </LinearLayout>

        <Button
            android:id="@+id/our_addinclass_activity_button"
            style="@style/long_blue_button"
            android:onClick="onClick"
            android:text="立即加入" />

    </LinearLayout>

</LinearLayout>
